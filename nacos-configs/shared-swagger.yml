knife4j:
  enable: true
  openapi:
    title: "${hm.swagger.title}"
    description: "${hm.swagger.title}"
    email: zhanghuyi@itcast.cn
    concat: 虎哥
    url: https://www.itcast.cn
    version: v1.0.0
    group:
      default:
        group-name: default
        api-rule: package
        api-rule-resources:
          - "${hm.swagger.package}"
